html
  head
    link(href='../css/mainstyle.css?var=1' type='text/css' rel='stylesheet')
    link(href='../css/loginstyle.css' type='text/css' rel='stylesheet')
    title loginpage
  body

    if message
      script.
        alert('로그인 에러!');
    if joinSign
      script.
        alert('회원가입 성공!');


    .top
      // ppl logo
      a(href='/')
        img#ppl_home(src='../icon/ppl_logo.png' alt='ppl')
      if user && user.id  
        .login_complete
          img#profileicon(src='../icon/mainpage_profileicon.png', alt='profile')
          a(href='/mypage')= user.name +'님'
          |             &nbsp;&nbsp;|&nbsp;&nbsp;
          a(href='/mypage') 설정
          |             &nbsp;&nbsp;|&nbsp;&nbsp;
          a(href='/auth/logout') 로그아웃
          //- 디자인 요청!!!
      else
        .login
          a(href='/auth') 로그인 / 회원가입
    // 메뉴바
    ul.mainmenu
      li#searchmenu.menu
        a(href='/search') SEARCH
      li#portfoliiomenu.menu
        a(href='/make') PORTFOLIO
      li#productmenu.menu
        a(href='/product') PRODUCT
    .loginbox
      h1.logintitle &#xB85C;&#xADF8;&#xC778;
      hr#topline
      // --form tag--
      form(action='/auth/login' method='post')
        .idbox
          img#idicon(src='../icon/loginpage_idicon.png' alt='idicon')
          input#idinput(name='nick' type='text' placeholder='  아이디')
        .pwbox
          img#pwicon(src='../icon/loginpage_pwicon.png' alt='pwicon')
          input#pwinput(name='password' type='password' placeholder='비밀번호')
        input#loginstate(type='checkbox')
        label(for='loginstate')
          span &#xB85C;&#xADF8;&#xC778; &#xC0C1;&#xD0DC; &#xC720;&#xC9C0;
        input.loginbtn(type='submit' value='')
      // -------------
      hr#bottomline
      a.findid(href='모름') &#xC544;&#xC774;&#xB514;/&#xBE44;&#xBC00;&#xBC88;&#xD638; &#xCC3E;&#xAE30;     
      |  |
      a.join(href='/auth/temp') &#xD68C;&#xC6D0;&#xAC00;&#xC785;
